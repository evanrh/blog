---
export const prerender = false;

import BaseLayout from "../../layouts/BaseLayout.astro";
import PostExcerptCard from "../../layouts/PostExcerptCard.astro";
import { getAllPosts } from "../../lib/wordpress";

const posts = await getAllPosts();
---

<BaseLayout title="Blog" description="Evan's musings">
  {posts.map((post) => <PostExcerptCard post={post} />)}
</BaseLayout>
